<Window x:Class="PDFOrtnerSorter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:PDFOrtnerSorter.ViewModels"
        xmlns:infra="clr-namespace:PDFOrtnerSorter.Infrastructure"
        mc:Ignorable="d"
        Title="PDF Ortner Sorter"
        Width="1400"
        Height="900"
        MinWidth="1024"
        MinHeight="720"
        Background="{DynamicResource AppBackground}"
        Foreground="White"
        FontFamily="Segoe UI">
    <Window.Resources>
        <LinearGradientBrush x:Key="AppBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#040914" Offset="0"/>
            <GradientStop Color="#0e213b" Offset="0.35"/>
            <GradientStop Color="#04060a" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="CardBrush" Color="#131d31"/>
        <SolidColorBrush x:Key="CardBrushAlt" Color="#1a2741"/>
        <LinearGradientBrush x:Key="AccentBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#8ef6d2" Offset="0"/>
            <GradientStop Color="#4ade80" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AccentBrushDark" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#66d49f" Offset="0"/>
            <GradientStop Color="#34c46c" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#cbd5f5"/>
        <SolidColorBrush x:Key="GhostBorderBrush" Color="#2a3244"/>
        <SolidColorBrush x:Key="GhostHoverBrush" Color="#343f55"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="26" ShadowDepth="0" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#04101f"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.35"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrushDark}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2fa95d"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10182b"/>
            <Setter Property="BorderBrush" Value="{StaticResource GhostBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource GhostHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#202a3f"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="BorderBrush" Value="#233044"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="MetricCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#19253a"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>

        <Style x:Key="CardToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource GhostButtonStyle}">
            <Setter Property="Padding" Value="12,6"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <infra:BooleanToTextConverter x:Key="BoolToTextConverter" TrueText="Ja" FalseText="Nein" />
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="PDF Ortner Sorter" FontSize="32" FontWeight="SemiBold" />
                <TextBlock Text="Sortiere massenhaft gescannte PDFs mit drei Klicks – Vorschau, Auswahl, Move."
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Einstellungen"
                    Command="{Binding OpenSettingsCommand}"
                    Margin="0,0,12,0"
                    Style="{StaticResource GhostButtonStyle}" />
                <Button Content="Aktualisieren"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,12,0"
                        Style="{StaticResource GhostButtonStyle}" />
                <Button Content="Auswahl verschieben"
                        Command="{Binding MoveSelectionCommand}"
                        Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <Border Style="{StaticResource CardStyle}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left">
                                <TextBlock Text="Ordner &amp; Batch" FontWeight="SemiBold" FontSize="17" />
                                <TextBlock Text="Verwalte Quelle, Ziel und Cache über das Einstellungs-Popup." Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
                            </StackPanel>
                            <Button Content="Einstellungen"
                                    Command="{Binding OpenSettingsCommand}"
                                    Style="{StaticResource GhostButtonStyle}"
                                    HorizontalAlignment="Right"
                                    DockPanel.Dock="Right" />
                        </DockPanel>
                        <Separator Margin="0,12,0,12" />
                        <TextBlock Text="Quellordner" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SourceFolder, TargetNullValue=Kein Ordner gewählt}"
                                   Foreground="White" TextWrapping="Wrap" Margin="0,2,0,12" />
                        <TextBlock Text="Zielbasis" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding DestinationBaseFolder, TargetNullValue=Kein Ordner gewählt}"
                                   Foreground="White" TextWrapping="Wrap" Margin="0,2,0,12" />
                        <TextBlock Text="Unterordnername" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding DestinationFolderName, TargetNullValue=Nicht gesetzt}" Foreground="White" Margin="0,2,0,12" />
                        <TextBlock>
                            <Run Text="Unterordner durchsuchen: " Foreground="{StaticResource TextSecondaryBrush}" />
                            <Run Text="{Binding IncludeSubfolders, Converter={StaticResource BoolToTextConverter}}" />
                        </TextBlock>
                        <TextBlock Margin="0,6,0,0">
                            <Run Text="Cache-Limit: " Foreground="{StaticResource TextSecondaryBrush}" />
                            <Run Text="{Binding PreviewCacheLimitMb, StringFormat={}{0} MB}" />
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Kennzahlen" FontWeight="SemiBold" FontSize="17" />
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock Text="Gefundene PDFs" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                                <TextBlock Text="{Binding Documents.Count}" FontSize="28" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Ausgewählt" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                                <TextBlock Text="{Binding SelectedCount, StringFormat={}{0} Dateien}" FontSize="18" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Margin="0,12,12,0">
                                <TextBlock Text="Cache-Limit" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                                <TextBlock Text="{Binding PreviewCacheLimitMb, StringFormat={}{0} MB}" FontSize="20" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,12,0,0">
                                <TextBlock Text="Quelle" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                                <TextBlock Text="{Binding SourceFolder}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Session" FontWeight="SemiBold" FontSize="17" />
                        <TextBlock Text="Status" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,12" />
                        <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" />
                        <Separator Margin="0,12,0,12" />
                        <TextBlock Text="{Binding SelectedCount, StringFormat={}{0} Dateien ausgewählt}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Documents.Count, StringFormat={}{0} gesamt}" Foreground="{StaticResource TextSecondaryBrush}" />
                        <Separator Margin="0,12,0,12" />
                        <TextBlock Text="Fortschritt" FontWeight="SemiBold" />
                        <ProgressBar Minimum="0" Maximum="1" Height="12" Value="{Binding MoveProgress}" Margin="0,6,0,0" />
                        <TextBlock Text="{Binding MoveProgress, StringFormat={}{0:P0}}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                        <Separator Margin="0,12,0,12" />
                        <TextBlock Text="Ziel" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding DestinationBaseFolder}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                        <TextBlock Text="{Binding DestinationFolderName}" Foreground="#cbd5f5" FontWeight="SemiBold" Margin="0,2,0,0" />
                        <Separator Margin="0,12,0,12" />
                        <Button Content="Auswahl verschieben"
                                Command="{Binding MoveSelectionCommand}"
                                Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Dokumente" FontSize="22" FontWeight="SemiBold" />
                            <TextBlock Text="Liste der gefundenen PDFs inklusive Vorschau."
                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                            <TextBlock Text="Shift+Klick erweitert die Auswahl, Strg+Klick toggelt einzelne Dateien."
                                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,6,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,6">
                                <Button Content="Alle markieren"
                                        Command="{Binding SelectAllDocumentsCommand}"
                                        Style="{StaticResource CardToolbarButtonStyle}"
                                        Margin="0,0,8,0" />
                                <Button Content="Auswahl leeren"
                                        Command="{Binding ClearSelectionCommand}"
                                        Style="{StaticResource CardToolbarButtonStyle}" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedCount, StringFormat={}{0} Dateien gewählt}" FontWeight="SemiBold" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding Documents.Count, StringFormat={}{0} gesamt}" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <ListView Grid.Row="1"
                              ItemsSource="{Binding Documents}"
                              SelectionMode="Extended"
                              ScrollViewer.CanContentScroll="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              BorderThickness="0"
                              Margin="0,12,0,0"
                              Background="Transparent">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0,0,0,6" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Cursor" Value="Hand" />
                                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnDocumentItemPreviewMouseLeftButtonDown" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.95" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#4ade80" BlurRadius="15" ShadowDepth="0" Opacity="0.35" />
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewModels:PdfDocumentViewModel}">
                                <Border x:Name="DocumentCard" Background="#162239" Padding="8" CornerRadius="10" BorderBrush="#21304a" BorderThickness="1">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,0,10,0" VerticalAlignment="Center" />
                                            <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                                <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding Location}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding DisplaySize}" Foreground="#cbd5f5" HorizontalAlignment="Right" />
                                                <TextBlock Text="PDF" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" HorizontalAlignment="Right" />
                                            </StackPanel>
                                        </Grid>

                                        <Grid Grid.Row="1" Margin="0,6,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition />
                                            </Grid.ColumnDefinitions>

                                            <ItemsControl Grid.Column="0" ItemsSource="{Binding Thumbnails}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Width="60" Height="80" Margin="0,0,8,0" Background="#0f172a" CornerRadius="6">
                                                            <Image Source="{Binding}" Stretch="Uniform" Margin="4"/>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>

                                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                <TextBlock Text="Vorschau wird erstellt..." FontStyle="Italic" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,12,0">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsPreviewLoaded}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="{Binding Document.LastWriteTimeUtc, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" />
                                            </StackPanel>
                                        </Grid>
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <Binding Path="IsSelected" RelativeSource="{RelativeSource AncestorType=ListViewItem}" />
                                        </DataTrigger.Binding>
                                        <Setter TargetName="DocumentCard" Property="Background" Value="#102a4c" />
                                        <Setter TargetName="DocumentCard" Property="BorderBrush" Value="#3b82f6" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <DockPanel Grid.Row="2" Margin="0,8,0,0" LastChildFill="False">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" Margin="0,0,12,0">
                <TextBlock Text="Letzte Aktualisierung:" Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Text="{Binding StatusMessage}" Foreground="White" TextTrimming="CharacterEllipsis" Width="600" Margin="8,0,0,0" />
            </StackPanel>
            <Border Background="#111827" Padding="10,4" CornerRadius="8" DockPanel.Dock="Right">
                <TextBlock Text="© 2025 PDF Ortner Sorter · made by Norphy" Foreground="{StaticResource TextSecondaryBrush}" />
            </Border>
        </DockPanel>
    </Grid>
</Window>
